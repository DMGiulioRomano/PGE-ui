# This repo has no build step: .jsx/.js are transpiled in-browser by Babel, so
# source is not meant to be auto-formatted by a stray editor "format on save".

# Vendored / generated — never format.
tests/node/node_modules/
.venv/
